var expect = require('expect');

describe('First Test', () => {
  it('Should properly run tests', () => {
    expect(1).toBe(1);
  });
});

describe('Second Test', () => {
  it('Should properly run tests', () => {
    expect(2).toBe(2);
  });
});
